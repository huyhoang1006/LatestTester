export const exportService: any = {
  buildDtoForCat: async (_cat: any, _ctx: any) => ({ flatMap: {}, arrayMap: {} }),
  extractFromMaps: (_cat: any, _flatMap: any, _arrayMap: any, _tableData: any) => ({})
}
